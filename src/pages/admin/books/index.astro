---
import AdminPanel from '../../../components/AdminPanel.svelte'
import Layout from '../../../layouts/Layout.astro'

const currentPage = Astro.url.pathname
---

<Layout>
  <AdminPanel navPage={currentPage} client:load/>
</Layout>