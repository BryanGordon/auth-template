---
import Layout from '../../../../layouts/Layout.astro'
import BookInfo from '../../../../components/BookInfo.svelte'

export const prerendered = false

const { id } = Astro.params
---

<Layout>
  <BookInfo bookId={id} client:load/>
</Layout>
